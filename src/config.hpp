#ifndef CONFIG_HPP
#define CONFIG_HPP

// Window settings
const unsigned int SCREEN_WIDTH = 1024;
const unsigned int SCREEN_HEIGHT = 768;

// Player physics settings
const float PLAYER_MASS = 1.2f;
const float PLAYER_FRICTION = 0.98f; // Damping factor, closer to 1 means less friction
const float PLAYER_MAX_SPEED = 400.0f;

// Mouse controller settings
const float MOUSE_FORCE = 500.0f;

#endif // CONFIG_HPP
